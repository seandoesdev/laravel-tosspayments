<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Toss Payments API 인증 정보
    |--------------------------------------------------------------------------
    |
    | Toss Payments 개발자 센터에서 발급받은 API 키를 설정합니다.
    | 테스트 환경과 프로덕션 환경의 키가 다르므로 환경변수로 관리하세요.
    |
    */

    'client_key' => env('TOSS_CLIENT_KEY', ''),
    'secret_key' => env('TOSS_SECRET_KEY', ''),

    /*
    |--------------------------------------------------------------------------
    | API 설정
    |--------------------------------------------------------------------------
    */

    'base_url' => env('TOSS_API_URL', 'https://api.tosspayments.com'),

    'version' => env('TOSS_API_VERSION', 'v2'),

    /*
    |--------------------------------------------------------------------------
    | HTTP 클라이언트 설정
    |--------------------------------------------------------------------------
    */

    'timeout' => env('TOSS_TIMEOUT', 30),

    'retry' => [
        'times' => env('TOSS_RETRY_TIMES', 3),
        'sleep' => env('TOSS_RETRY_SLEEP', 100), // milliseconds
    ],

    /*
    |--------------------------------------------------------------------------
    | SSL 검증 설정
    |--------------------------------------------------------------------------
    |
    | 로컬/테스트 환경에서 SSL이 없는 경우 false 로 비활성화할 수 있습니다.
    | 프로덕션에서는 반드시 true를 권장합니다.
    |
    */

    'ssl_verify' => env('TOSS_SSL_VERIFY', true),

    /*
    |--------------------------------------------------------------------------
    | 웹훅 설정
    |--------------------------------------------------------------------------
    */

    'webhook' => [
        'secret' => env('TOSS_WEBHOOK_SECRET', ''),
        'tolerance' => env('TOSS_WEBHOOK_TOLERANCE', 300), // seconds
    ],
];
